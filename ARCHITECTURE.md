# معماری «مسیر تحول اقتصاد»

## تجربه

- یک مسیر پیوسته تک‌صفحه‌ای؛ هر فصل با اسکرول وارد صحنه بعدی می‌شود.
- روایت سه‌لایه: جمله کوتاه، تصویر/حرکت، سپس مثال یا سؤال ذهنی.
- تصویر سینمایی اصلی در لایه پس‌زمینه ثابت است و با GSAP به‌آرامی حرکت می‌کند.
- پیشرفت کاربر در `localStorage` ذخیره می‌شود و دکمه آغاز، ادامه مسیر را ممکن می‌کند.

## اجزا

- `StoryExperience`: کنترل روایت، پیشرفت، فصل فعال و ScrollTriggerها.
- `Scene`: قاب مشترک فصل‌ها و تم رنگی هر دوره.
- `Reveal`: ورود مرحله‌ای محتوای هر ضرب داستانی.
- `BankDashboard`: درون تجربه اصلی، پنج وضعیت تعاملی برای بانک‌های رشد.
- `ProgressRail`: پیشرفت کلی و فصل جاری.

## توسعه آینده

- صدا: کنترل آن از حالا در نوار بالا پیش‌بینی شده؛ فایل‌های محیطی می‌توانند به هر Scene متصل شوند.
- ویدئو: هر Scene می‌تواند در آینده به‌جای `scene-orb` یک ویدئوی کوتاه lazy-loaded بگیرد.
- تصویر: دارایی اصلی در `public/economy-evolution.png` مستقل از کد است و بدون تغییر کامپوننت قابل جایگزینی است.
